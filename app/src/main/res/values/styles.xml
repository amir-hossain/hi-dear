<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/brand_color</item>
        <item name="colorSecondary">@color/brand_color</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:actionMenuTextColor">@android:color/white</item>
        <item name="drawerArrowStyle">@style/DrawerArrowStyle</item>
    </style>

    <style name="DrawerArrowStyle" parent="@style/Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item>
        <item name="color">@color/brand_secondary_color</item>
    </style>

    <style name="timerStyle" parent="@android:style/TextAppearance.Medium">
        <item name="fontFamily">roboto_thin</item>
        <item name="android:textColor">@color/gray</item>
    </style>

    <style name="splashText" parent="timerStyle">
        <item name="fontFamily">roboto_thin</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="whiteBtn" parent="Widget.AppCompat.TextView">
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@drawable/white_round</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="grayBtn" parent="whiteBtn">
        <item name="android:background">@drawable/gray_round</item>
    </style>
</resources>